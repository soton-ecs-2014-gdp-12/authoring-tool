'use strict';

describe('authoringTool.authoring module', function() {

  beforeEach(module('authoringTool.authoring'));

  describe('authoring controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('AuthoringCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
